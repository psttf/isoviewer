- блокировать повторное открытие файла -- вставить код из тестового проекта по
 MSDI, вставлять как есть, в generic переделать не удалось
-- проверять изменение файла на диске
- в установщике BISO.h и т. п. берутся не из того места
-- похоже, что они не нужны, попробовать без них

---

- выгрузка в RDF отключена, т. к. не компилируется SemWeb